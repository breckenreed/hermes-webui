# Test-only dependencies. Nothing here ships in the image — the Dockerfile
# installs requirements.txt alone, so the runtime stays at four packages.
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt -r requirements-dev.txt
#   .venv/bin/pytest
pytest>=8.0
httpx>=0.27          # fastapi.testclient is built on it
pyflakes>=3.0        # the lint step in .github/workflows/tests.yml
